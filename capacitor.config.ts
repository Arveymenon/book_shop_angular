import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.schooby.shop',
  appName: 'Schooby_frontend',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
